var enderecoModel = require("../models/enderecoModel");

// function listarPorEmpresa(req, res) {
//   var cnpj = req.query.cnpj;

//   enderecoModel.listarPorEmpresa(cnpj).then((resultado) => {
//     res.status(200).json(resultado);
//   });
// }

module.exports = {
  listarPorEmpresa
};
